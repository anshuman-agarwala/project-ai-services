worker:
  image: icr.io/ai-services-private/ai-services:anshuman7
  runtime: "openshift"
  token: ""
  gatewayAddr: ""
  # @generate:password length=32, special=false
  mtlsEncryptionKey: ""
  resources:
    requests:
      memory: "1Gi"
      cpu: "250m"
    limits:
      memory: "1Gi"
      cpu: "1000m"
  tls:
    storage: 10Mi
